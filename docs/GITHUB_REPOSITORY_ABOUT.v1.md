# CANONICAL SOURCE

# GITHUB_REPOSITORY_ABOUT v1

**Purpose:** Suggested text for the GitHub repository **About** / description field (Settings → General). Git does not store this metadata; keep this file so the wording stays aligned with canon.

---

## Copy-paste (repository description)

Use as a single paragraph or first line + details in the extended description:

```text
General-purpose security contour + optional guard framework.
Defines trust chain, secrets, host hardening, incident response, and future execution boundaries.
Observe-first, zero decision authority, adapter-based, system-agnostic.
```

**One-line variant (short About field):**

```text
General-purpose security contour + optional guard framework; observe-first; zero decision authority; adapter-based; system-agnostic.
```

---

**Version:** v1
